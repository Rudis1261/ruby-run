# RubyConsole.com

### Just some stuffs I need to document
- Installation
- Required GEMS

### Running a server
```bin/server -Ilib```

### Running a server in Daemon mode
```bin/server -Ilib -l server.log -d```